import { useEffect, useMemo, useRef, useState } from 'react';
import PositionsPanel from './components/PositionsPanel';
import AlertsPanel from './components/alerts/AlertsPanel';
import ScannerPanel from './components/scanner/ScannerPanel';
import WatchlistPanel from './components/watchlist/WatchlistPanel';
import JournalPopup from './components/journal/JournalPopup';
import ArtifactModal from './components/artifact/ArtifactModal';
import { useArtifactStore } from './components/artifact/useArtifactStore';

type GridArea = { colStart:number; colEnd:number; rowStart:number; rowEnd:number };
type PanelState = { newPanel:boolean; watchlist:boolean; positions:boolean; scanners:boolean; newPanel2:boolean };
const COL_WIDTH=500,TOTAL_COLS=10,TOTAL_ROWS=6,ROW_HEIGHT=155,SHEET_HEIGHT=TOTAL_ROWS*ROW_HEIGHT;
const basePanel: React.CSSProperties = {background:'#060606',minWidth:0,minHeight:0,overflow:'hidden',border:'1px solid #141414',boxSizing:'border-box',position:'relative'};
const areaStyle=(a:GridArea):React.CSSProperties=>({gridColumn:`${a.colStart} / ${a.colEnd}`,gridRow:`${a.rowStart} / ${a.rowEnd}`});
function getLayout(expanded: PanelState){ const newPanel=expanded.newPanel?{colStart:1,colEnd:3,rowStart:1,rowEnd:7}:{colStart:2,colEnd:3,rowStart:1,rowEnd:7}; const watchlist=expanded.watchlist?{colStart:3,colEnd:5,rowStart:1,rowEnd:7}:{colStart:3,colEnd:4,rowStart:1,rowEnd:7}; const scanners=expanded.scanners?{colStart:7,colEnd:9,rowStart:1,rowEnd:7}:{colStart:8,colEnd:9,rowStart:1,rowEnd:7}; const newPanel2=expanded.newPanel2?{colStart:9,colEnd:11,rowStart:1,rowEnd:7}:{colStart:9,colEnd:10,rowStart:1,rowEnd:7}; const centerStart=watchlist.colEnd, centerEnd=scanners.colStart; const infoHub=expanded.positions?{colStart:centerStart,colEnd:centerEnd,rowStart:1,rowEnd:2}:{colStart:centerStart,colEnd:centerEnd,rowStart:1,rowEnd:3}; const positions=expanded.positions?{colStart:centerStart,colEnd:centerEnd,rowStart:2,rowEnd:7}:{colStart:centerStart,colEnd:centerEnd,rowStart:3,rowEnd:7}; return {newPanel,watchlist,infoHub,positions,scanners,newPanel2}; }
export default function App(){ const [expanded,setExpanded]=useState<PanelState>({newPanel:false,watchlist:false,positions:false,scanners:false,newPanel2:false}); const [netLiq,setNetLiq]=useState<number|null>(null); const [bp,setBp]=useState<number|null>(null); const [positionCount,setPositionCount]=useState(0); const [quoteCount,setQuoteCount]=useState(0); const [isJournalOpen,setIsJournalOpen]=useState(false); const viewportRef=useRef<HTMLDivElement|null>(null); const artifactStore=useArtifactStore(); const layout=useMemo(()=>getLayout(expanded),[expanded]);
useEffect(()=>{ const load=()=>fetch('/api/balances').then(r=>r.json()).then(d=>{const row=Array.isArray(d?.balances)&&d.balances[0]?d.balances[0]:null; setNetLiq(row?.net_liquidating_value ?? null); setBp(row?.option_buying_power ?? row?.buying_power ?? null);}).catch(()=>{}); load(); const ws=new WebSocket(`${window.location.protocol==='https:'?'wss':'ws'}://${window.location.host}/ws/account`); ws.onmessage=(e)=>{try{const msg=JSON.parse(e.data); if(msg.type==='account_snapshot'){ setPositionCount(Array.isArray(msg.positions)?msg.positions.length:0); const row=Array.isArray(msg.balances)&&msg.balances[0]?msg.balances[0]:null; setNetLiq(row?.net_liquidating_value ?? null); setBp(row?.option_buying_power ?? row?.buying_power ?? null); }}catch{}}; return ()=>ws.close();},[]);
useEffect(()=>{ const ws=new WebSocket(`${window.location.protocol==='https:'?'wss':'ws'}://${window.location.host}/ws/market`); ws.onmessage=()=>setQuoteCount(c=>c+1); return ()=>ws.close();},[]);
useEffect(()=>{ const el=viewportRef.current; if(!el) return; const center=()=>{el.scrollLeft=Math.max(0,(el.scrollWidth-el.clientWidth)/2);}; center(); const t=setTimeout(center,50); window.addEventListener('resize',center); return ()=>{clearTimeout(t); window.removeEventListener('resize',center);}; },[expanded]);
const applyPreset=(preset:'default'|'preset2'|'preset3')=>{ if(preset==='default') return setExpanded({newPanel:false,watchlist:false,positions:false,scanners:false,newPanel2:false}); if(preset==='preset2') return setExpanded({newPanel:true,watchlist:true,positions:false,scanners:true,newPanel2:true}); setExpanded({newPanel:true,watchlist:false,positions:true,scanners:false,newPanel2:true});};
const togglePanel=(panel:keyof PanelState)=>setExpanded(prev=>({...prev,[panel]:!prev[panel]}));
return <><div style={{background:'#000',color:'#fff',minHeight:'100vh',height:'100vh',overflow:'hidden',fontFamily:'Arial, Helvetica, sans-serif'}}><div style={{height:34,display:'flex',alignItems:'center',justifyContent:'space-between',padding:'0 10px',background:'#0b0b0b',borderBottom:'1px solid #1d1d1d',boxSizing:'border-box'}}><div style={{fontSize:13,letterSpacing:'0.05em',color:'#ddd'}}>Granite Tasty Skeleton</div><div style={{display:'flex',gap:8}}><button onClick={()=>setIsJournalOpen(true)} style={{border:'1px solid #2a2a2a',background:'#080808',color:'#ddd',padding:'4px 8px',fontSize:10,cursor:'pointer'}}>Journal</button><button onClick={()=>artifactStore.openArtifact({symbol:'SPY',source:{type:'synthetic'}})} style={{border:'1px solid #2a2a2a',background:'#080808',color:'#ddd',padding:'4px 8px',fontSize:10,cursor:'pointer'}}>Artifact Test</button><button onClick={()=>applyPreset('default')} style={{border:'1px solid #2a2a2a',background:'#080808',color:'#ddd',padding:'4px 8px',fontSize:10,cursor:'pointer'}}>Default State</button><button onClick={()=>applyPreset('preset2')} style={{border:'1px solid #2a2a2a',background:'#080808',color:'#ddd',padding:'4px 8px',fontSize:10,cursor:'pointer'}}>Outer + Watchlist/Scanners</button><button onClick={()=>applyPreset('preset3')} style={{border:'1px solid #2a2a2a',background:'#080808',color:'#ddd',padding:'4px 8px',fontSize:10,cursor:'pointer'}}>Outer + Positions</button></div></div><div ref={viewportRef} style={{height:'calc(100vh - 34px)',overflowX:'auto',overflowY:'hidden',background:'#0a0a0a'}}><div style={{width:TOTAL_COLS*COL_WIDTH,height:SHEET_HEIGHT,margin:'0 auto',display:'grid',gridTemplateColumns:`repeat(${TOTAL_COLS}, ${COL_WIDTH}px)`,gridTemplateRows:`repeat(${TOTAL_ROWS}, ${ROW_HEIGHT}px)`,background:'#050505'}}><div style={{...basePanel,...areaStyle(layout.newPanel),background:'#fd18fa',display:'flex',flexDirection:'column'}}><div className='panel-head'><span>{expanded.newPanel?'ALERT CENTER - EXPANDED':'ALERT CENTER - COLLAPSED'}</span><button onClick={()=>togglePanel('newPanel')}>{expanded.newPanel?'−':'+'}</button></div><div style={{flex:1,minHeight:0,overflow:'hidden'}}><AlertsPanel /></div></div><div style={{...basePanel,...areaStyle(layout.watchlist),background:'#15e80f',display:'flex',flexDirection:'column'}}><div className='panel-head'><span>{expanded.watchlist?'WATCHLIST - EXPANDED':'WATCHLIST - COLLAPSED'}</span><button onClick={()=>togglePanel('watchlist')}>{expanded.watchlist?'−':'+'}</button></div><div style={{flex:1,minHeight:0}}><WatchlistPanel onLaunchArtifact={(payload)=>artifactStore.openArtifact({symbol: payload.symbol, source:{type:'synthetic'}})} /></div></div><div style={{...basePanel,...areaStyle(layout.infoHub),background:'#ff2618',display:'grid',gridTemplateColumns:'repeat(4,minmax(0,1fr))',gap:8,padding:12}}><div style={{background:'rgba(255,255,255,0.14)',padding:12}}><div>Net Liq</div><div style={{fontSize:26,fontWeight:700}}>{netLiq !== null ? `$${netLiq.toLocaleString()}` : '—'}</div></div><div style={{background:'rgba(255,255,255,0.14)',padding:12}}><div>BP</div><div style={{fontSize:26,fontWeight:700}}>{bp !== null ? `$${bp.toLocaleString()}` : '—'}</div></div><div style={{background:'rgba(255,255,255,0.14)',padding:12}}><div>Positions</div><div style={{fontSize:26,fontWeight:700}}>{positionCount}</div></div><div style={{background:'rgba(255,255,255,0.14)',padding:12}}><div>Quote Ticks</div><div style={{fontSize:26,fontWeight:700}}>{quoteCount}</div></div></div><div style={{...basePanel,...areaStyle(layout.positions),background:'#4d84da',display:'flex',flexDirection:'column'}}><div className='panel-head'><span>{expanded.positions?'POSITIONS - EXPANDED':'POSITIONS - COLLAPSED'}</span><button onClick={()=>togglePanel('positions')}>{expanded.positions?'−':'+'}</button></div><div style={{flex:1,minHeight:0}}><PositionsPanel /></div></div><div style={{...basePanel,...areaStyle(layout.scanners),background:'#e1ca68',display:'flex',flexDirection:'column'}}><div className='panel-head'><span>{expanded.scanners?'SCANNERS - EXPANDED':'SCANNERS - COLLAPSED'}</span><button onClick={()=>togglePanel('scanners')}>{expanded.scanners?'−':'+'}</button></div><div className='panel-stack' style={{minHeight:0}}><div className='stack-box' style={{alignItems:'stretch',justifyContent:'stretch',padding:0}}><div style={{width:'100%',height:'100%',overflow:'hidden'}}><ScannerPanel /></div></div><div className='stack-box'>VOL SURFACE VIA ARTIFACT</div><div className='stack-box'>ROLL SCANNER LIVE</div></div></div><div style={{...basePanel,...areaStyle(layout.newPanel2),background:'#2fc8d3',display:'flex',flexDirection:'column'}}><div className='panel-head'><span>{expanded.newPanel2?'NEW PANEL 2 - EXPANDED':'NEW PANEL 2 - COLLAPSED'}</span><button onClick={()=>togglePanel('newPanel2')}>{expanded.newPanel2?'−':'+'}</button></div><div className='panel-fill' style={{padding:10,color:'#082f33',fontSize:11,display:'grid',alignContent:'start',gap:6}}><div style={{fontWeight:700}}>SYSTEM STATUS</div><div>Quotes websocket: live</div><div>Account websocket: live</div><div>Alerts backend: live</div><div>Artifact modal: {artifactStore.open ? 'Open' : 'Closed'}</div></div></div></div></div></div><JournalPopup open={isJournalOpen} onClose={()=>setIsJournalOpen(false)} /><ArtifactModal open={artifactStore.open} payload={artifactStore.payload} onClose={artifactStore.closeArtifact} /></> }
