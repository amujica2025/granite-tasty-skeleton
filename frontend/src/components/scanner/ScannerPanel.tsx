import { useEffect, useMemo, useState } from 'react';
import { buildEntryCandidates, buildRollCandidates } from './scannerUtils';
import type { ChainRow, PositionRow, ScannerCandidate, ScannerMode } from './scannerTypes';
export default function ScannerPanel(){ const [mode,setMode]=useState<ScannerMode>('entry'); const [symbol,setSymbol]=useState('SPY'); const [chain,setChain]=useState<ChainRow[]>([]); const [positions,setPositions]=useState<PositionRow[]>([]); useEffect(()=>{ fetch(`/api/chain?symbol=${symbol}`).then(r=>r.json()).then(d=>setChain(Array.isArray(d?.rows)?d.rows:[])).catch(()=>setChain([])); },[symbol]); useEffect(()=>{ const load=()=>fetch('/api/positions').then(r=>r.json()).then(d=>setPositions(Array.isArray(d?.positions)?d.positions:[])).catch(()=>setPositions([])); load(); const ws=new WebSocket(`${window.location.protocol==='https:'?'wss':'ws'}://${window.location.host}/ws/account`); ws.onmessage=()=>load(); return ()=>ws.close(); },[]); const candidates: ScannerCandidate[] = useMemo(()=> mode==='entry'?buildEntryCandidates(chain,symbol):buildRollCandidates(positions,chain,symbol),[mode,chain,positions,symbol]); return <div style={{padding:12,overflow:'auto',height:'100%'}}><div style={{display:'flex',gap:8,marginBottom:8}}><button onClick={()=>setMode('entry')}>Entry</button><button onClick={()=>setMode('roll')}>Roll</button><input value={symbol} onChange={e=>setSymbol(e.target.value.toUpperCase())}/></div><table style={{width:'100%',fontSize:11}}><thead><tr><th>Symbol</th><th>Structure</th><th>Credit</th><th>Debit</th><th>Net</th><th>BP</th></tr></thead><tbody>{candidates.map((c,i)=><tr key={i}><td>{c.symbol}</td><td>{c.structure}</td><td>{c.credit.toFixed(2)}</td><td>{c.debit.toFixed(2)}</td><td>{c.net.toFixed(2)}</td><td>{c.bpEffect.toFixed(0)}</td></tr>)}</tbody></table></div>; }
